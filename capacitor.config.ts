import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.xkonstix.mathrush",
  appName: "Math Rush",
  webDir: "out",
  ios: {
    contentInset: "always",
    backgroundColor: "#F4F1EA",
  },
};

export default config;
